def factorial(n):
    if n == 1:
        return 1
    else:
        return n * factorial(n - 1)

txt = "Factorial of {} is {}"
num = int(input("Enter a number to find its factorial:"))
print(txt.format(num,factorial(num)))
